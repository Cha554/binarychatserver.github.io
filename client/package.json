{
  "name": "binarychat-client",
  "version": "1.0.0",
  "description": "BinaryChat end-to-end encrypted CLI client",
  "bin": "client.js",
  "scripts": {
    "start": "node client.js",
    "build": "echo 'Use ./build-unix.sh (Linux/Mac) or build-windows.bat (Windows) to compile a binary instead of npm run build' && exit 1"
  },
  "dependencies": {
    "tweetnacl": "^1.0.3",
    "tweetnacl-util": "^0.15.1",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "postject": "^1.0.0-alpha.6"
  }
}
